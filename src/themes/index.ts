type Theme = {
  colors: {
    background: string
    link: {
      active: string
      hover: string
      normal: string
    }
    text: {
      normal: string
      callout: string
    }
    primary: string
    secondary: string
  }
}

export default Theme
